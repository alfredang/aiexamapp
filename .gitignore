# macOS
.DS_Store

# Xcode
build/
DerivedData/
*.xcuserstate
xcuserdata/
*.xcscmblueprint
*.xccheckout
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Xcode project user/workspace state
*.xcodeproj/project.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/

# Swift Package Manager
.build/
.swiftpm/

# CocoaPods / Carthage (if ever added)
Pods/
Carthage/Build/

# Secrets / signing
*.p12
*.mobileprovision
.env
.env.local

# Selectively commit .claude/skills + agents + commands (project context). Ignore the rest.
.claude/*
!.claude/skills/
!.claude/agents/
!.claude/commands/
!.claude/skills/**
!.claude/agents/**
!.claude/commands/**

# EXCEPTION: the create-env sub-skill holds ASC secrets (Apple ID password, key/issuer
# ids) — keep it local-only, never push to GitHub. Re-ignored after the un-ignore above.
.claude/skills/app-store-submission/create-env/

# Playwright MCP session artifacts
.playwright-mcp/
ai-exams-availability-set.png
__pycache__/
